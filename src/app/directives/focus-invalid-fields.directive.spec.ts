import { FocusInvalidFieldsDirective } from './focus-invalid-fields.directive';

describe('FocusInvalidFieldsDirective', () => {
  it('should create an instance', () => {
    const directive = new FocusInvalidFieldsDirective();
    expect(directive).toBeTruthy();
  });
});
